import 'package:flutter/material.dart';

class splashScreen extends StatefulBuilder {
  const splashscreen({super.key});

  @override
Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(title: const Text("appname")),

  );
}
}